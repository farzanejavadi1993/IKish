/*
package ikish.aftab.ws.android.Adapters

import android.widget.TextView
import com.github.siyamed.shapeimageview.RoundedImageView


class BindingAdapter {

    @BindingAdapter("setImageView")
    fun setImageView(imageView: RoundedImageView?, src: Int) {
        if (imageView != null) {

            imageView.setImageResource(src)



        }
    }

    @androidx.databinding.BindingAdapter("textPrice")
    fun textPrice(textView: TextView?, price: Int) {
        if (textView != null) {
            textView.text = String.format("%,d", price) + " تومان "
        }
    }
}*/
